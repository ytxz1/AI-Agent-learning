"""
Few-Shot Prompting (少样本提示) 示例
===================================

少样本提示是指在提示中提供 1-5 个示例，帮助模型理解任务模式。
模型通过学习示例中的模式来完成新任务。
"""

from openai import OpenAI
from dotenv import load_dotenv
import os

load_dotenv()


# ============================================================
# 1. 基础配置
# ============================================================
# 初始化 OpenAI 客户端
client = OpenAI(
    api_key=os.getenv('OPENAI_API_KEY'),  # 从 .env 文件读取 API 密钥
    base_url='https://api.deepseek.com'               # DeepSeek API 地址
)


# ============================================================
# 2. 少样本提示示例
# ============================================================
# 少样本提示的核心：通过示例教会模型任务模式
# 这里使用情感分析任务，提供了 2 个示例，让模型学习后处理新输入
messages = [
    {
        "role": "user",
        "content": """
输入：今天真开心
输出：积极

输入：我很难过
输出：消极

输入：今天压力很大
输出：
"""
    }
]

# 调用 API
response = client.chat.completions.create(
    model="deepseek-chat",  # 使用的模型名称
    messages=messages,       # 包含示例的消息列表
)

# 输出模型的回答
# 预期输出："消极"（模型会根据示例学习到的模式进行判断）
print(response.choices[0].message.content)


# ============================================================
# 详细解释
# ============================================================
"""
什么是少样本提示 (Few-Shot Prompting)?
======================================

1. 核心概念
   ----------
   少样本提示通过在提示中提供少量示例（通常 1-5 个），
   帮助模型理解任务的输入输出模式，然后处理新的输入。

   在本示例中：
   - 提供了 2 个情感分析的示例：
     * "今天真开心" -> "积极"
     * "我很难过" -> "消极"
   - 然后给出新输入："今天压力很大"
   - 模型会学习示例中的模式，推断出输出应为"消极"

2. 工作原理
   ----------
   - 模型通过上下文学习 (In-Context Learning) 理解任务模式
   - 示例展示了输入和输出的对应关系
   - 模型将这种模式应用到新的输入上
   - 无需微调模型，仅通过提示就能完成任务

3. 与零样本提示的区别
   -------------------
   - 零样本：不提供示例，直接描述任务
   - 少样本：提供示例，让模型学习任务模式
   - 少样本通常比零样本效果更好，尤其是对于格式要求严格的任务

4. 示例数量的选择
   ----------------
   - 1 个示例 (One-Shot)：简单任务，快速验证
   - 2-3 个示例：大多数任务的最佳平衡点
   - 4-5 个示例：复杂任务或需要覆盖多种情况
   - 注意：示例过多会消耗更多 token，且可能引入噪声

5. 本代码的结构解析
   -----------------
   messages 中的内容格式：

   ```
   输入：[示例输入1]
   输出：[示例输出1]

   输入：[示例输入2]
   输出：[示例输出2]

   输入：[新输入]
   输出：
   ```

   模型会自动识别这个模式，并为新输入生成对应的输出。

6. 适用场景
   ----------
   - 分类任务（情感分析、主题分类等）
   - 格式转换（JSON、CSV、Markdown 等）
   - 文本生成（特定风格、特定格式）
   - 任务描述难以用语言清晰表达时

7. 优点
   ------
   - 比零样本更准确，模型能更好地理解任务
   - 可以控制输出格式（通过示例展示期望格式）
   - 适用于各种任务类型

8. 局限性
   --------
   - 需要准备高质量的示例
   - 示例数量受上下文长度限制
   - 示例的选择会影响模型表现
   - 消耗更多 token

9. 最佳实践
   ----------
   - 示例要具有代表性，覆盖常见情况
   - 示例格式要一致，便于模型学习模式
   - 示例数量适中，通常 2-3 个即可
   - 可以在示例后添加明确的指令，进一步引导模型

10. 扩展示例
    ---------

    # 文本分类（3 个示例）
    输入：今天股市大涨
    输出：财经

    输入：新款 iPhone 发布
    输出：科技

    输入：足球比赛结果
    输出：体育

    输入：房价持续上涨
    输出：

    # 风格转换（2 个示例）
    输入：今天天气不错
    输出：今日风和日丽，天朗气清

    输入：我很饿
    输出：腹中空空如也，甚是饥肠辘辘

    输入：这个东西很好吃
    输出：

    # JSON 格式提取（2 个示例）
    输入：张三，25岁，北京
    输出：{"name": "张三", "age": 25, "city": "北京"}

    输入：李四，30岁，上海
    输出：{"name": "李四", "age": 30, "city": "上海"}

    输入：王五，28岁，广州
    输出：

11. 与思维链 (Chain-of-Thought) 的结合
    ------------------------------------
    少样本提示可以与思维链结合，在示例中展示推理过程：

    输入：小明有 5 个苹果，给了小红 2 个，还剩几个？
    输出：小明原本有 5 个苹果，给了小红 2 个，所以剩下 5 - 2 = 3 个苹果。

    输入：小华有 8 支铅笔，买了 3 支，又丢了 1 支，现在有几支？
    输出：

    （详见 cot_demo.py）
"""
