Day23 的目标是把 Agent 项目接口化。

核心任务：
1. 将 Agent 封装成 FastAPI 服务。
2. 提供普通 JSON 问答接口。
3. 提供流式响应接口，模拟大模型逐字输出。
4. 添加统一错误处理，避免接口直接暴露混乱异常。
5. 配置 CORS 跨域，让前端页面可以调用后端 API。

接口化的意义：
- 前端可以通过 HTTP 调用 Agent。
- 移动端、小程序、企业系统都可以复用 Agent 能力。
- 项目更容易部署、测试和维护。

Day23 推荐记住一句话：
脚本只能自己跑，API 才能被别人调用。
